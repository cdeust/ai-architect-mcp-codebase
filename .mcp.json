{
  "mcpServers": {
    "ai-architect": {
      "command": "python3",
      "args": [
        "-c",
        "import json,os,subprocess,sys; d=json.load(open(os.path.expanduser('~/.claude/plugins/installed_plugins.json')))['plugins']; keys=['ai-architect-codebase@ai-architect-marketplace','automatised-pipeline@automatised-pipeline-marketplace']; p=next((d[k][0]['installPath'] for k in keys if k in d),None); p or sys.exit('ai-architect: plugin install path not found'); b=os.path.join(p,'target','release','ai-architect-mcp-codebase'); (not os.access(b,os.X_OK)) and subprocess.run([os.path.join(p,'bin','ensure-binary.sh'),'verbose'],stderr=sys.stderr); os.access(b,os.X_OK) or (sys.stderr.write(f'ai-architect: FATAL — binary missing after ensure-binary.sh; build manually with: cargo build --release --manifest-path \"{os.path.join(p,\"Cargo.toml\")}\"\\n') or sys.exit(1)); os.execvp(b,[b])"
      ]
    }
  }
}
